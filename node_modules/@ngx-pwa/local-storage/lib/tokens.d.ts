import { InjectionToken, Provider } from '@angular/core';
export declare const LOCAL_STORAGE_PREFIX: InjectionToken<string>;
export interface LocalStorageProvidersConfig {
    /** Optional prefix to avoid collision in multiple apps on same subdomain */
    prefix?: string;
}
export declare function localStorageProviders(config: LocalStorageProvidersConfig): Provider[];
